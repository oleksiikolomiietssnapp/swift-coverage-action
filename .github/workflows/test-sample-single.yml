name: Test Sample (Single Job)

on:
  workflow_call:

jobs:
  coverage:
    permissions:
      contents: read
      pull-requests: write
      issues: write
    uses: ./.github/workflows/swift-coverage.yml
    with:
      macos-version: "latest"
      xcode-version: ""
      project-name-override: "SampleMultiTarget"
      working-directory: "./sample"
      coverage-comment-header: "### 📊 Sample Coverage Report (Single Job)"
      post-comment: true
      coverage-threshold: "70"
      fail-on-low-coverage: true
